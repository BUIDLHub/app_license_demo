import {connect} from 'react-redux';
import Wiz from './Wizard';

const s2p = state => {
    return {
        
    }
}

const d2p = dispatch => {
    return {

    }
}

export default connect(s2p, d2p)(Wiz);